<Window x:Class="Прокат_авто.ContractDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали договора" 
        Height="700" 
        Width="850"
        WindowStartupLocation="CenterOwner">

	<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
		<Grid Margin="20">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<!-- Информация о договоре -->
			<Border Grid.Row="0" Background="#1A237E" CornerRadius="5" Padding="15" Margin="0,0,0,15">
				<StackPanel>
					<TextBlock x:Name="txtContractTitle" FontSize="18" FontWeight="Bold" Foreground="White" TextWrapping="Wrap"/>
					<TextBlock x:Name="txtContractStatus" FontSize="14" Foreground="#BDC3C7" Margin="0,5,0,0" TextWrapping="Wrap"/>
				</StackPanel>
			</Border>

			<!-- Детали аренды -->
			<!-- Детали аренды -->
			<Border Grid.Row="1" Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,15">
				<StackPanel>
					<TextBlock Text="📋 Детали аренды" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<StackPanel Grid.Column="0">
							<TextBlock TextWrapping="Wrap">
                    <Bold>Автомобиль: </Bold>
                    <Run x:Name="txtCarName"/>
							</TextBlock>
							<TextBlock Margin="0,10,0,0" TextWrapping="Wrap">
                    <Bold>Дата начала: </Bold>
                    <Run x:Name="txtStartDate"/>
							</TextBlock>
							<TextBlock TextWrapping="Wrap">
                    <Bold>Дата окончания: </Bold>
                    <Run x:Name="txtEndDate"/>
							</TextBlock>
							<TextBlock TextWrapping="Wrap">
                    <Bold>Количество дней: </Bold>
                    <Run x:Name="txtDaysCount"/>
							</TextBlock>
						</StackPanel>

						<StackPanel Grid.Column="1">
							<TextBlock TextWrapping="Wrap">
                    <Bold>Стоимость в день: </Bold>
                    <Run x:Name="txtRentalAmount" Foreground="#27AE60"/>
							</TextBlock>
							<TextBlock Margin="0,10,0,0" TextWrapping="Wrap">
                    <Bold>Итоговая сумма: </Bold>
                    <Run x:Name="txtTotalAmount" Foreground="#E74C3C" FontSize="16"/>
							</TextBlock>
						</StackPanel>
					</Grid>
				</StackPanel>
			</Border>

			<!-- Скидки и штрафы -->
			<Border Grid.Row="2" Background="#F8F9FA" CornerRadius="5" Padding="15" Margin="0,0,0,15">
				<Grid>
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<Border Grid.Column="0" Background="#2ECC71" CornerRadius="5" Padding="10" Margin="0,0,10,0">
						<StackPanel>
							<TextBlock Text="💰 Скидки" FontWeight="Bold" Foreground="White" FontSize="14"/>
							<ItemsControl x:Name="discountsList" Margin="0,10,0,0" MaxHeight="150">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<TextBlock Foreground="White" Margin="0,2" TextWrapping="Wrap">
                                            <Run Text="• "/>
                                            <Run Text="{Binding Title}"/>
                                            <Run Text=" - "/>
                                            <Run Text="{Binding Percent}"/>
                                            <Run Text="%"/>
										</TextBlock>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
							<TextBlock x:Name="txtTotalDiscount" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" TextWrapping="Wrap"/>
						</StackPanel>
					</Border>

					<Border Grid.Column="1" Background="#E74C3C" CornerRadius="5" Padding="10">
						<StackPanel>
							<TextBlock Text="⚠️ Штрафы" FontWeight="Bold" Foreground="White" FontSize="14"/>
							<ItemsControl x:Name="penaltiesList" Margin="0,10,0,0" MaxHeight="150">
								<ItemsControl.ItemTemplate>
									<DataTemplate>
										<TextBlock Foreground="White" Margin="0,2" TextWrapping="Wrap">
                                            <Run Text="• "/>
                                            <Run Text="{Binding Title}"/>
                                            <Run Text=" - "/>
                                            <Run Text="{Binding Summ}"/>
                                            <Run Text=" руб."/>
										</TextBlock>
									</DataTemplate>
								</ItemsControl.ItemTemplate>
							</ItemsControl>
							<TextBlock x:Name="txtTotalPenalties" Foreground="White" FontWeight="Bold" Margin="0,10,0,0" TextWrapping="Wrap"/>
						</StackPanel>
					</Border>
				</Grid>
			</Border>

			<!-- Платежи -->
			<!-- Платежи -->
			<Border Grid.Row="3" Background="#F8F9FA" CornerRadius="5" Padding="15">
				<StackPanel>
					<Grid Margin="0,0,0,10">
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>

						<TextBlock Grid.Column="0" Text="💳 История платежей" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
						<Button Grid.Column="1" Content="➕Добавить" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" FontWeight="Bold" FontSize="13"
                    Click="btnAddPayment_Click"/>
					</Grid>

					<DataGrid x:Name="paymentsGrid" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" Margin="0,0,0,15"
                  MaxHeight="250" MinHeight="100">
						<DataGrid.Columns>
							<DataGridTextColumn Header="Дата платежа" Binding="{Binding PaymentDate}" Width="*"/>
							<DataGridTextColumn Header="Сумма" Binding="{Binding Amount}" Width="*"/>
							<DataGridTextColumn Header="Метод оплаты" Binding="{Binding PaymentMethod}" Width="*"/>
							<DataGridTextColumn Header="Тип" Binding="{Binding PaymentType}" Width="*"/>
						</DataGrid.Columns>
					</DataGrid>

					<Border Background="#3498DB" CornerRadius="5" Padding="10">
						<Grid>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>

							<TextBlock Grid.Row="0" Grid.Column="0" Text="Осталось оплатить:" 
                           Foreground="White" FontSize="14" VerticalAlignment="Center" FontWeight="Bold"/>
							<TextBlock x:Name="txtFinalAmount" Grid.Row="0" Grid.Column="1" FontSize="20" 
                           FontWeight="Bold" Foreground="White" Margin="10,0,0,0" TextWrapping="Wrap"/>
						</Grid>
					</Border>
				</StackPanel>
			</Border>
		</Grid>
	</ScrollViewer>
</Window>